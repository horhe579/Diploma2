# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
accountant:
  username: accountant
  email: accountant@kpmg.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

manager:
  username: manager
  email: manager@kpmg.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

eavesdropper:
  username: eavesdropper
  email: eavesdropper@pwc.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

temp_user:
  username: temp
  email: user@rogue.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

temp_user2:
  username: temp2
  email: user2@rogue.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

temp_user3:
  username: temp3
  email: user@wizard.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

temp_user4:
  username: temp4
  email: user@paladin.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

temp_user5:
  username: temp5
  email: user@barbarian.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
