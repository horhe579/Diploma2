<div class="empty-state empty-state--only-child">
  <p class="empty-state__text">
    You don't have any cards yet!
  </p>

  <%= link_to "Add card",
               new_card_path,
                class: "btn btn--primary",
                data: { turbo_frame: dom_id(Card.new) } %>
</div>