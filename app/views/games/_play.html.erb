
<div class="container">
  <div class="header">
    <h1>Game</h1>
  </div>

  <% if GamesUser.find_by(user_id: current_user.id, game: @game).completed_at != nil %>
    Seems like you lost!
  <% else %>
    <%= render partial: "games/hand" %>
  <% end %>






</div>
